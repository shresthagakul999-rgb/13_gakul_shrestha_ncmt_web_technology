QN 1
Network protocols are a set of rules that are responsible for the communication of data between various devices in the network.

QN 2
A Web Browser (like Chrome, Firefox) is the software you use to access and view the internets content, acting as your window to the web; a Search Engine (like Google, Bing) is a website/service within the browser that finds specific information by indexing the web.

QN 3
The World Wide Web (WWW or "the Web") is a vast system of interconnected documents, resources, and websites accessed via the Internet, using browsers and protocols like HTTP to link information through hyperlinks, allowing users to navigate text, images, videos, and more to share and find data globally.

QN 4
A static web page shows fixed content that is the same for all users and does not change unless the developer manually edits the HTML file.dynamic web page generates content at runtime using scripts and databases, so the information can change based on user input, time, or data.
